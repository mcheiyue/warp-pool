# Plan: v0.5 可控聚合池（Momus 返工修订）

## Goal

聚合口可运维：成员 in/out 不杀进程；全局粘性 sticky；`GET/POST /pool*`；health-once 门禁；WebUI 开关 + sticky。

## Evidence

- aggregate 只读 `healthy.json` RR（main.go L119-255）  
- 入池仅 health + unique（health-once.sh）  
- 无 pooled / sticky 字段  
- product：单聚合口为主，直连正交；sticky = API 全局，非用户名路由（已否决）；pooled ⊥ sticky

## Acceptance (P0) — 可跑步骤

- [ ] N=2 双健康+unique，token 知，AGG 口可达，直连 `11000+i`  
- [ ] unpool id=1 → 聚合多次取样均为 id0.v4；id1 直连仍通  
- [ ] pool 回 id=1 → members 长度 2  
- [ ] 旧 meta 无 pooled 键 → 视为 true；is_pool_eligible 正确  
- [ ] 删实例回归无残留（目录/netns 消失，healthy.json 无该 id）

## Acceptance (P1 sticky) — 可跑步骤

- [ ] `POST /pool/sticky` body {"id":0} → 聚合取样全走 id0.v4  
- [ ] DELETE /pool/sticky → 回 RR  
- [ ] sticky id 出池 → 选路回退 RR（文件保留）  
- [ ] `GET /pool/sticky` 返回文件内容或 `{"id":null}`

## Work packages

### WP-P0 — 成员可控

1. `lib.sh`：`write_meta` **merge** 运维字段（pooled/exclude_reason 默认保留）；加 `is_pool_eligible`  
2. `health-once.sh`：backends 写入前 `is_pool_eligible`；pooled=false 不入 backends  
3. `main.go`：`GET /pool`（join meta.v4）、`POST /pool/membership`（改 meta + `SUPERVISE_RESTART=0 health-once`）、`GET /instances` 补缺省 pooled  
4. WebUI：入池开关 + 聚合成员/排除列表  
5. `tests/smoke-pool.sh`：覆盖 §10 可跑步骤  
6. VPS 旁路验证

### WP-P1 — 粘性 + 运维

1. `main.go`：`sticky.json` + `getSticky()` + `serveSocks` 选路（含拨号失败回退）  
2. `main.go`：`GET/POST/DELETE /pool/sticky`（body only）  
3. WebUI：实例卡「固定到此」+ 聚合面板 sticky + 清除  
4. `rotate-instance.sh`：P1 临时 unpool（exclude_reason=drain）→ rotate → 恢复  
5. `agg_enabled` 状态文件 + config 白名单（P0 恒开）  
6. `pool.log`  
7. AGENT/README/ADR

### WP-P2 — 可选

1. weight + weighted RR

## Out of scope

源 IP hash、目标域名绑定、独立 drain 布尔、多池、热改 AGG 端口、API 代理业务流量、具体业务专用文档

## Implementation order

1. 落文档（本文件 + pivot）— done  
2. 用户说 go 后：P0 → P1 → 按需 P2 → GHCR  

Do **not** start code until user explicitly says implement.
